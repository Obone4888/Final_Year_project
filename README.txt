To run this app double click the Run_tool appliction exe and then when a windows allow access button shows select accept this allows the server to run on a port.
All files and other executables for the Client and Server are within their respective files that are shown these are all the nesscery CPP and header files that are needed for this project.

Inside of the Server Files there are the packeges and SQLite required to use the SQL and Query the Database